# Copy this file to .env or .env.local and set values appropriately
# Vite exposes variables prefixed with VITE_

# WebSocket endpoint for live playlist updates
VITE_SOCKET_URL=wss://livesync.dgtoohl.com/ws

# Optional: Explicit API base for production builds only.
# In development we rely on Vite proxy and use relative '/api'.
VITE_API_BASE_URL=https://livesync.dgtoohl.com

# Dev-only: Enable Vite proxy targets to avoid CORS during local dev.
# Defaults to live server if not set:
VITE_PROXY_API_TARGET=https://livesync.dgtoohl.com
VITE_PROXY_WS_TARGET=wss://livesync.dgtoohl.com
